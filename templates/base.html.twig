<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="https://bootswatch.com/5/journal/bootstrap.min.css">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{path('app_home')}}">Home
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item me-3 ms-3">
                            <a class="nav-link btn text-light" href="{{ path('app_logout') }}"><strong>LOGOUT</strong></a>
                        </li>

                        <a href="{{ path('app_profile') }}" class=" btn btn-secondary text-decoration-none text-dark text-capitalize ">{{ app.user.username }}</a>

                    {% else %}

                        <li class="nav-item me-3 ms-3">
                            <a class="nav-link btn text-light" href="{{ path('app_register') }}"><strong>REGISTER</strong></a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link btn text-light" href="{{ path('app_login') }}"><strong>LOGIN</strong></a>
                        </li>

                    {% endif %}

                    {% if app.user %}

                    {% if "ROLE_ADMIN" in app.user.roles %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_index') }}">Promote/demote</a>
                    </li>
{% endif %}
{% endif %}
                </ul>
                <form class="d-flex">
                    <input class="form-control me-sm-2" type="search" placeholder="Search">
                    <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                </form>
            </div>
        </div>
    </nav>


    <div class="container">
    {% block body %}

    {% endblock %}
    </div>
    </body>
</html>
